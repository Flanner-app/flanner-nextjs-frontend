import clsx from 'clsx'

const Home = () => {
  return (
    <main className="grow p-8">
      <label
        className={clsx(
          'block h-10 w-24 rounded-xl border-2 border-black-default bg-white px-4 py-2',
          'shadow-brutalism',
        )}
      ></label>
    </main>
  )
}

export default Home
